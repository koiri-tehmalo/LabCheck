# 🔬 LabCheck
ระบบจัดการและตรวจสอบครุภัณฑ์สำหรับห้องปฏิบัติการออนไลน์ (Laboratory Equipment Management System) พร้อมระบบจัดการสิทธิ์แบบ Role-Based Access Control (RBAC) พัฒนาด้วย Next.js (App Router), Prisma ORM และ PostgreSQL รองรับการสร้าง QR Code ประจำครุภัณฑ์เพื่อการเข้าถึงข้อมูลอย่างรวดเร็ว

🎯 คุณสมบัติ (Features)
✅ ระบบยืนยันตัวตน: ล็อกอินและลงทะเบียนผู้ใช้งานอย่างปลอดภัยด้วย NextAuth.js (JWT)
✅ จัดการครุภัณฑ์: เพิ่ม แก้ไข ลบข้อมูลครุภัณฑ์ พร้อมระบุโมเดล สถานะ (ใช้งานได้/ชำรุด/สูญหาย) และสถานที่ติดตั้ง
✅ จัดการชุดครุภัณฑ์: จัดกลุ่มครุภัณฑ์ที่ใช้งานร่วมกันเพื่อความสะดวกในการจัดการ
✅ ระบบ QR Code: สแกน QR Code ประจำครุภัณฑ์เพื่อดูรายละเอียดข้อมูลได้ทันที (รองรับบุคคลทั่วไปโดยไม่ต้องล็อกอิน)
✅ แดชบอร์ดสรุปผล: หน้ารวมภาพรวมสถิติ จำนวนสถานะของครุภัณฑ์ด้วยกราฟิกที่ดูง่าย และกิจกรรมล่าสุด
✅ จัดการผู้ใช้: ผู้ดูแลระบบสามารถจัดการบุคคลากร ปรับเปลี่ยน Role และการมอบสิทธิ์ได้ทันที
✅ ระบบ Permission: แบ่งสิทธิ์การใช้งานชัดเจนตาม Role เช่น Admin (จัดการระบบรวม) และ Staff (เพิ่ม/แก้ไขครุภัณฑ์ทั่วไป)
✅ Responsive Design: UI/UX สวยงามทันสมัยสไตล์ Glassmorphism ทำงานได้ดีทั้งบนคอมพิวเตอร์ แท็บเล็ต และมือถือ

🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
Frontend / Backend: Next.js 15 (App Router), React 19
Database: PostgreSQL
Containerization: Docker + Docker Compose
ORM: Prisma ORM
Authentication: NextAuth.js
Styling: Tailwind CSS, Radix UI (shadcn/ui), Lucide Icons
Form Validation: React Hook Form + Zod

🚀 การติดตั้ง (Installation)
1. Clone โปรเจกต์
```bash
git clone https://github.com/your-username/LabCheck.git
cd LabCheck
```

2. ติดตั้ง Dependencies
```bash
npm install
```

3. ตั้งค่า Environment Variables
สร้างไฟล์ `.env` ไว้ที่โฟลเดอร์หลักของโปรเจกต์ (Root):
```bash
cp .env.example .env
```

แก้ไขค่าภายในไฟล์ `.env` ให้ตรงกับฐานข้อมูลของคุณ:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/labcheck?schema=public"
NEXTAUTH_SECRET="your_nextauth_secret_key"
NEXTAUTH_URL="http://localhost:9002"
```

4. รันฐานข้อมูลด้วย Docker
```bash
docker compose up -d
```

5. ตั้งค่าฐานข้อมูลและสร้างรูปแบบ (Schema)
```bash
npx prisma db push
npx prisma generate
```

6. รันโปรเจกต์
```bash
npm run dev
```
เปิดเบราว์เซอร์ไปที่ http://localhost:9002

👤 บัญชีทดสอบ (Test Accounts)
Username|Password|Role
---|---|---
`admin@gmail.com`|`password123`|ผู้ดูแลระบบ (ADMIN)
`staff@gmail.com`|`password123`|พนักงาน (STAFF)

🔐 ระบบ Permission
Role|ความหมาย
---|---
`ADMIN`|ใช้งานได้ทุกฟีเจอร์ (เพิ่ม/แก้ไข/ลบครุภัณฑ์, ลบผู้ใช้, ปรับระดับสิทธิ์ผู้ใช้)
`STAFF`|ดูข้อมูล เพิ่มและแก้ไขครุภัณฑ์ได้ แต่ไม่สามารถลบข้อมูล หรือจัดการบัญชีผู้ใช้งานท่านอื่นได้

📁 โครงสร้างโปรเจกต์
```
LabCheck/
├── prisma/               # Schema และ Migrations ของ Database
├── public/               # รูปภาพและไฟล์สแตติก (Static Assets)
└── src/
    ├── actions/          # Server Actions จัดการ Logic ฝั่งเซิร์ฟเวอร์ และ Query API
    ├── app/              # หน้าเว็บไซต์และ Routing (Next.js App Router)
    ├── components/       # UI Components แบ่งเป็นส่วน Dashboard, Layout, UI อเนกประสงค์ (shadcn)
    ├── hooks/            # Custom Hooks (เช่น useAuth, useMobile)
    └── lib/              # ฟังก์ชันช่วยเหลือ (Utils), Config ฐานข้อมูลและระบบ Auth
```

👨‍💻 ผู้พัฒนา
ชื่อผู้พัฒนา: มูฮัมหมัดคอยรี เต๊ะมาลอ
📧 Email: khayreetehmalo@gmail.com